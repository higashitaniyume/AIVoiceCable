<UserControl x:Class="AIVoiceCable.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <Button DockPanel.Dock="Top" HorizontalAlignment="Left" Content="清空界面日志" Command="{Binding ClearCommand}" />
        <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss.fff}}" Width="180" />
                <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80" />
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*" />
                <DataGridTextColumn Header="异常摘要" Binding="{Binding Exception}" Width="320" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
